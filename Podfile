# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CreateObservable' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CreateObservable
  pod 'RxSwift'
  pod 'RxCocoa'
  target 'CreateObservableTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CreateObservableUITests' do
    # Pods for testing
  end

end
